function blockOfCodes() {
    let color = "red";
    if (color === "red")
        //we print this console.log because the statement is true
        console.log("tomato");
    else
        // we ignore this console.log because the statement is true
        console.log("banana");

    //this console.log is out of the if statement and we will print it
    console.log("bye");

}

blockOfCodes()