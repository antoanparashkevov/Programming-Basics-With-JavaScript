function conditionalStatement() {
    let a = 5;
    let b = 6;

    // in this parentheses we put the condition who always returns Boolean value
    if (a === b) {

        //we put running code here if the conditional is fulfilled
        //fulfilled - изпълнено
        console.log("equal")
    }
    //the result is true or false. In this case = false, therefore nothing to print in the console
}