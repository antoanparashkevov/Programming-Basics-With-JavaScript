function create() {
    //we are going to create a new variable

    //let is a key word, let is initialization
    //name is the name of the variable.
    //Todor is the value of the name variable

    //var is the second initialization and it is valid in the whole function scope.
    //But var carry some bigger problems and we can overwrite values without wanting to.
    let name = "Todor";
    console.log(name);
}
create();