function squareArea(input) {

    //we bring the fist index of the array ( 5 )
    let a = Number(input[0]);

    //we multiply the variable ( a )
    let area = a * a;

    //we print the variable
    console.log(area);
}

squareArea(["5"]);
